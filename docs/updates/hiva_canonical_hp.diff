--- /mnt/data/hiva_hp_original/configuration_smolvla_hiva_coeff.py	2026-05-05 22:10:24.230945778 +0000
+++ /mnt/data/configuration_smolvla_hiva_coeff.py	2026-05-05 22:14:10.697757139 +0000
@@ -46,6 +46,16 @@
     hiva_coeff_sidecar_summary_path: str | None = DEFAULT_HIVA_COEFF_SUMMARY
     hiva_normalize_coefficients: bool = True
     hiva_coeff_norm_eps: float = 1e-6
+    # Coefficient basis/decode mode:
+    # - "duration_specific": legacy mode. Each duration d uses its own d x K basis at
+    #   sidecar fitting and inference decode time. A wrong duration can therefore select a wrong basis.
+    # - "canonical_hp": canonical Dmax hold-pad mode. All sidecar coefficients are fit against
+    #   the same Dmax x K basis after the GT target is hold-padded after duration d*. During inference
+    #   the model always decodes a Dmax-step chunk with this canonical basis; duration only chooses how
+    #   many prefix actions are queued for execution.
+    # - "canonical_mt": same architecture as canonical_hp, but intended for a max-target sidecar ablation
+    #   whose coefficients fit the real Dmax future actions instead of the hold-padded safe target.
+    hiva_basis_mode: str = "duration_specific"
 
     # B.4 loss weights.
     hiva_tr_loss_weight: float = 1.0
@@ -54,18 +64,17 @@
     hiva_duration_noisy_loss_weight: float = 1.0
     hiva_duration_noisy_sigma: float = 0.25
     # Duration CE variants:
-    # - "ce_mean": simple mean CE over the batch. This is the default for the cleaner-suffix ablation,
-    #   where the duration token cannot attend coefficient tokens and is trained from prefix context.
-    # - "mean": preserves the original mean(CE * noisy_weight) behavior.
-    # - "duration_noisy_weights": normalizes by sum(noisy_weight), i.e. sum(CE*w)/sum(w).
-    hiva_duration_loss: str = "ce_mean"
+    # - "ce_mean": simple mean CE over the batch. This is the default cleaner-suffix ablation.
+    # - "mean": legacy mean(CE * exp(-t^2/sigma^2)).
+    # - "duration_noisy_weights": normalized weighted CE, sum(CE*w)/sum(w).
+    hiva_duration_loss: str | None = None
     duration_loss: str | None = None
     # Coefficient suffix attention variants:
-    # - "duration_prefix": [duration][coeffs] uses mask [1,1,0,...,0]. The duration token attends
-    #   only to prefix/context plus itself; coefficient tokens attend prefix, duration, and all coeffs.
-    # - "full": [1,0,...,0], one bidirectional suffix block where duration and coeffs attend each other.
-    # - "causal": [1,1,...,1], original causal suffix ordering for debugging only.
-    hiva_suffix_attention: str = "duration_prefix"
+    # - "duration_prefix": suffix [duration][coeffs] uses mask [1,1,0,...,0]. The duration token
+    #   attends prefix/context plus itself; coefficient tokens attend prefix, duration, and all coeffs.
+    # - "full": mask [1,0,...,0], one bidirectional suffix block.
+    # - "causal": mask [1,1,...,1], original causal/block-ordering debug mode.
+    hiva_suffix_attention: str | None = None
 
     # Base SmolVLA initialization.
     init_smolvla_checkpoint_path: str | None = DEFAULT_INIT_SMOLVLA_CHECKPOINT
@@ -78,6 +87,13 @@
         self.duration_classes = self.hiva_duration_classes
         if self.duration_loss is not None:
             self.hiva_duration_loss = self.duration_loss
+        if self.hiva_duration_loss is None:
+            # Backward compatible default: older weighted-duration scripts used lambda < 1.
+            # New cleaner-suffix runs generally use lambda=1 and should default to CE mean.
+            self.hiva_duration_loss = "mean" if self.hiva_duration_noisy_loss_weight < 1.0 else "ce_mean"
+        if self.hiva_suffix_attention is None:
+            self.hiva_suffix_attention = "duration_prefix" if self.hiva_duration_loss == "ce_mean" else "full"
+        self.hiva_basis_mode = str(self.hiva_basis_mode)
 
         if len(self.hiva_duration_classes) < 2:
             raise ValueError("`hiva_duration_classes` must contain at least two durations.")
@@ -125,6 +141,11 @@
                 "`hiva_suffix_attention` must be one of: 'duration_prefix', 'full', 'causal'. "
                 f"Got {self.hiva_suffix_attention!r}."
             )
+        if self.hiva_basis_mode not in ("duration_specific", "canonical_hp", "canonical_mt"):
+            raise ValueError(
+                "`hiva_basis_mode` must be one of: 'duration_specific', 'canonical_hp', 'canonical_mt'. "
+                f"Got {self.hiva_basis_mode!r}."
+            )
 
     @property
     def action_delta_indices(self) -> list:
--- /mnt/data/hiva_hp_original/modeling_smolvla_hiva_coeff.py	2026-05-05 22:10:24.231651947 +0000
+++ /mnt/data/modeling_smolvla_hiva_coeff.py	2026-05-05 22:11:38.251345819 +0000
@@ -345,6 +345,7 @@
         self.register_buffer("hiva_grip_std", stat_tensor("grip", "std", 1, 1.0), persistent=True)
 
     def _register_basis_buffers(self) -> None:
+        # Always keep the legacy duration-specific bases so older sidecars/checkpoints continue to work.
         for duration in self.config.hiva_duration_classes:
             basis = _clamped_bspline_basis(
                 int(duration),
@@ -353,9 +354,23 @@
             )
             self.register_buffer(f"hiva_basis_{duration}", basis, persistent=False)
 
+        # Canonical HP/MT modes use one Dmax x K basis for all samples and all inference durations.
+        canonical_basis = _clamped_bspline_basis(
+            int(self.config.hiva_dmax),
+            n_ctrl=self.config.hiva_k,
+            degree=self.config.hiva_degree,
+        )
+        self.register_buffer("hiva_basis_canonical", canonical_basis, persistent=False)
+
+    def _uses_canonical_basis(self) -> bool:
+        return self.config.hiva_basis_mode in ("canonical_hp", "canonical_mt")
+
     def _basis(self, duration: int, *, device, dtype) -> Tensor:
         return getattr(self, f"hiva_basis_{int(duration)}").to(device=device, dtype=dtype)
 
+    def _canonical_basis(self, *, device, dtype) -> Tensor:
+        return self.hiva_basis_canonical.to(device=device, dtype=dtype)
+
     def normalize_coeffs(self, targets: HiVACoeffTargets) -> HiVACoeffTargets:
         if not self.config.hiva_normalize_coefficients:
             return targets
@@ -388,28 +403,22 @@
         return torch.exp(-(time.to(dtype=torch.float32) ** 2) / (sigma**2))
 
     def _hiva_suffix_att_list(self, suffix_len: int) -> list[int]:
-        """Return the 1D block-attention mask for the HiVA coefficient suffix.
-
-        The suffix order is always
-            [duration] [translation coeffs] [rotation coeffs] [gripper coeffs].
+        """Return the block-attention mask for [duration][tr][rot][grip] suffix tokens.
 
-        SmolVLA's ``make_att_2d_masks`` interprets a 1 as the start of a new block and a
-        0 as sharing the previous block. Therefore:
-        - ``duration_prefix`` => [1,1,0,...,0]. The duration token can read only the
-          multimodal prefix plus itself, while coefficient tokens can read the duration
-          token and all coefficient tokens bidirectionally.
-        - ``full`` => [1,0,...,0]. All HiVA suffix tokens share one bidirectional block.
-        - ``causal`` => [1,1,...,1]. Debug-only original causal ordering.
+        SmolVLA's ``make_att_2d_masks`` interprets 1 as starting a new cumulative
+        attention block and 0 as sharing the previous block.
         """
         if suffix_len < 2:
             raise ValueError(f"HiVA suffix must contain duration plus coefficient tokens, got {suffix_len}.")
-
         mode = self.config.hiva_suffix_attention
         if mode == "duration_prefix":
+            # Duration attends prefix + itself; coefficient tokens attend prefix + duration + all coeffs.
             return [1, 1] + [0] * (suffix_len - 2)
         if mode == "full":
+            # One bidirectional suffix block: duration and coefficient tokens all attend each other.
             return [1] + [0] * (suffix_len - 1)
         if mode == "causal":
+            # Original block-causal ordering for debugging only.
             return [1] * suffix_len
         raise ValueError(f"Unknown hiva_suffix_attention={mode!r}.")
 
@@ -549,20 +558,16 @@
         )
 
         if self.config.hiva_duration_loss == "ce_mean":
-            # New cleaner-suffix ablation: supervise duration with simple mean CE over the whole batch.
-            # This pairs with hiva_suffix_attention="duration_prefix", where the duration token reads
-            # prefix multimodal context but cannot read noisy coefficient tokens.
             duration_noisy_loss = duration_ce
             duration_noisy_loss_reduced = duration_ce.mean()
         elif self.config.hiva_duration_loss == "duration_noisy_weights":
             duration_noisy_loss = duration_weighted_loss_normalized_per_sample
-            duration_noisy_loss_reduced = (
-                duration_weighted_loss_per_sample.sum() / duration_noisy_weight_sum
-            )
-        else:  # "mean": legacy mean(CE * w(t)) behavior.
+            duration_noisy_loss_reduced = duration_weighted_loss_per_sample.sum() / duration_noisy_weight_sum
+        elif self.config.hiva_duration_loss == "mean":
             duration_noisy_loss = duration_weighted_loss_per_sample
             duration_noisy_loss_reduced = duration_weighted_loss_per_sample.mean()
-
+        else:
+            raise ValueError(f"Unknown hiva_duration_loss={self.config.hiva_duration_loss!r}.")
         duration_noisy_loss_scaled_reduced = (
             self.config.hiva_duration_noisy_loss_weight * duration_noisy_loss_reduced
         )
@@ -584,13 +589,17 @@
             (duration_correct * duration_noisy_weights).sum() / duration_noisy_weight_sum
         )
         duration_noisy_weight_sq_sum = (duration_noisy_weights * duration_noisy_weights).sum().clamp_min(1e-6)
-        duration_noisy_weight_ess = (duration_noisy_weight_sum * duration_noisy_weight_sum) / duration_noisy_weight_sq_sum
+        duration_noisy_weight_ess = (
+            duration_noisy_weight_sum * duration_noisy_weight_sum / duration_noisy_weight_sq_sum
+        )
         fm_total_reduced = (
             self.config.hiva_tr_loss_weight * tr_loss.mean()
             + self.config.hiva_rot_loss_weight * rot_loss.mean()
             + self.config.hiva_grip_loss_weight * grip_loss.mean()
         )
-        duration_to_fm_ratio = duration_noisy_loss_scaled_reduced / fm_total_reduced.detach().clamp_min(1e-6)
+        duration_to_fm_ratio = (
+            duration_noisy_loss_scaled_reduced / fm_total_reduced.detach().clamp_min(1e-6)
+        )
 
         def duration_acc_for_time_mask(mask: Tensor) -> Tensor:
             count = mask.sum()
@@ -647,6 +656,7 @@
             "hiva_duration_noisy_sigma": self.config.hiva_duration_noisy_sigma,
             "hiva_duration_loss": self.config.hiva_duration_loss,
             "hiva_suffix_attention": self.config.hiva_suffix_attention,
+            "hiva_basis_mode": self.config.hiva_basis_mode,
         }
         return per_sample_loss, loss_dict
 
@@ -742,10 +752,55 @@
         raw_actions = self.decode_coefficients_to_raw_actions(coeffs_raw, duration_steps)
         return raw_actions, duration_steps
 
+    def _decode_basis_to_raw_actions(self, coeffs_raw: HiVACoeffTargets, phi: Tensor) -> Tensor:
+        """Decode raw LIBERO actions from one B-spline basis shared by the whole batch.
+
+        ``phi`` has shape [T, K]. Translation and rotation coefficients are cumulative labels;
+        gripper coefficients decode the absolute continuous command directly.
+        """
+        bsize = coeffs_raw["tr"].shape[0]
+        device = coeffs_raw["tr"].device
+        dtype = coeffs_raw["tr"].dtype
+        horizon = int(phi.shape[0])
+        raw_actions = torch.zeros(
+            bsize,
+            self.config.hiva_dmax,
+            self.config.action_feature.shape[0],
+            dtype=dtype,
+            device=device,
+        )
+        eye = torch.eye(3, dtype=dtype, device=device)
+
+        p_hat = torch.einsum("dk,bkc->bdc", phi, coeffs_raw["tr"])
+        p0 = torch.zeros(bsize, 1, 3, dtype=dtype, device=device)
+        tr_delta = torch.diff(torch.cat([p0, p_hat], dim=1), dim=1)
+
+        rho_hat = torch.einsum("dk,bkc->bdc", phi, coeffs_raw["rot"])
+        rot_mats = _rotvec_to_matrix(rho_hat.reshape(-1, 3)).reshape(bsize, horizon, 3, 3)
+        prev = torch.cat([eye.expand(bsize, 1, 3, 3), rot_mats[:, :-1]], dim=1)
+        delta = prev.transpose(-1, -2) @ rot_mats
+        raw_rot = _matrix_to_rotvec(delta.reshape(-1, 3, 3)).reshape(bsize, horizon, 3)
+        raw_rot = raw_rot / float(self.config.hiva_rot_scale_eta)
+
+        grip = torch.einsum("dk,bkc->bdc", phi, coeffs_raw["grip"]).clamp(-1.0, 1.0)
+        decoded = torch.cat([tr_delta, raw_rot, grip], dim=-1)
+        raw_actions[:, :horizon, : decoded.shape[-1]] = decoded
+        return raw_actions
+
     def decode_coefficients_to_raw_actions(self, coeffs_raw: HiVACoeffTargets, duration_steps: Tensor) -> Tensor:
-        bsize = duration_steps.shape[0]
+        """Decode coefficients to raw LIBERO action chunks.
+
+        Legacy ``duration_specific`` mode decodes with the basis selected by predicted duration.
+        Canonical HP/MT modes always decode a full Dmax-step chunk with the canonical Dmax basis;
+        duration only controls the number of queued/executed actions in ``select_action``.
+        """
         device = duration_steps.device
         dtype = coeffs_raw["tr"].dtype
+        if self._uses_canonical_basis():
+            phi = self._canonical_basis(device=device, dtype=dtype)
+            return self._decode_basis_to_raw_actions(coeffs_raw, phi)
+
+        bsize = duration_steps.shape[0]
         raw_actions = torch.zeros(
             bsize,
             self.config.hiva_dmax,
@@ -753,36 +808,18 @@
             dtype=dtype,
             device=device,
         )
-        eye = torch.eye(3, dtype=dtype, device=device)
 
         for duration in self.config.hiva_duration_classes:
             mask = duration_steps == int(duration)
             if not mask.any():
                 continue
             phi = self._basis(int(duration), device=device, dtype=dtype)
-            tr_theta = coeffs_raw["tr"][mask]
-            rot_theta = coeffs_raw["rot"][mask]
-            grip_theta = coeffs_raw["grip"][mask]
-
-            p_hat = torch.einsum("dk,bkc->bdc", phi, tr_theta)
-            p0 = torch.zeros(p_hat.shape[0], 1, 3, dtype=dtype, device=device)
-            tr_delta = torch.diff(torch.cat([p0, p_hat], dim=1), dim=1)
-
-            rho_hat = torch.einsum("dk,bkc->bdc", phi, rot_theta)
-            rot_mats = _rotvec_to_matrix(rho_hat.reshape(-1, 3)).reshape(-1, int(duration), 3, 3)
-            prev = torch.cat(
-                [
-                    eye.expand(rot_mats.shape[0], 1, 3, 3),
-                    rot_mats[:, :-1],
-                ],
-                dim=1,
-            )
-            delta = prev.transpose(-1, -2) @ rot_mats
-            raw_rot = _matrix_to_rotvec(delta.reshape(-1, 3, 3)).reshape(-1, int(duration), 3)
-            raw_rot = raw_rot / float(self.config.hiva_rot_scale_eta)
-
-            grip = torch.einsum("dk,bkc->bdc", phi, grip_theta).clamp(-1.0, 1.0)
-            decoded = torch.cat([tr_delta, raw_rot, grip], dim=-1)
-            raw_actions[mask, : int(duration), : decoded.shape[-1]] = decoded
+            sub_coeffs = {
+                "tr": coeffs_raw["tr"][mask],
+                "rot": coeffs_raw["rot"][mask],
+                "grip": coeffs_raw["grip"][mask],
+            }
+            decoded = self._decode_basis_to_raw_actions(sub_coeffs, phi)
+            raw_actions[mask, : int(duration), : decoded.shape[-1]] = decoded[:, : int(duration)]
 
         return raw_actions
--- /mnt/data/hiva_hp_original/hiva_coeff_sidecar.py	2026-05-05 22:10:24.232032190 +0000
+++ /mnt/data/hiva_coeff_sidecar.py	2026-05-05 22:14:29.230937228 +0000
@@ -31,6 +31,20 @@
         "hiva_real_steps",
         "hiva_has_synthetic_tail",
         "hiva_tail_mode",
+        "hiva_basis_mode",
+        "hiva_target_mode",
+        "hiva_basis_dmax",
+        "hiva_basis_k",
+        "hiva_basis_degree",
+        "hiva_prefix_tr_rmse",
+        "hiva_prefix_rot_rmse",
+        "hiva_prefix_grip_rmse",
+        "hiva_prefix_action_rmse",
+        "hiva_wrong_long_tr_energy",
+        "hiva_wrong_long_rot_energy",
+        "hiva_wrong_long_grip_energy",
+        "hiva_wrong_long_action_energy",
+        "hiva_tail_motion_energy",
     )
 
     def __init__(
@@ -41,12 +55,14 @@
         n_ctrl: int,
         duration_classes: tuple[int, ...],
         require_full_coverage: bool = True,
+        expected_basis_mode: str | None = None,
     ):
         self.dataset = dataset
         self.sidecar_path = Path(sidecar_path)
         self.n_ctrl = int(n_ctrl)
         self.duration_classes = tuple(int(d) for d in duration_classes)
         self.require_full_coverage = bool(require_full_coverage)
+        self.expected_basis_mode = expected_basis_mode
 
         rows = self._load_rows(self.sidecar_path)
         if not rows:
@@ -72,6 +88,22 @@
             if key in self._by_episode_frame:
                 raise ValueError(f"Duplicate (episode_index, frame_index)={key} in {self.sidecar_path}.")
 
+            if self.expected_basis_mode:
+                row_basis_mode_value = row.get("hiva_basis_mode")
+                if row_basis_mode_value in (None, ""):
+                    if self.expected_basis_mode != "duration_specific":
+                        raise ValueError(
+                            "Configured canonical HiVA coefficient mode requires a sidecar with "
+                            f"hiva_basis_mode={self.expected_basis_mode!r}, but the column/value is missing."
+                        )
+                else:
+                    row_basis_mode = str(row_basis_mode_value)
+                    if row_basis_mode != self.expected_basis_mode:
+                        raise ValueError(
+                            f"Sidecar hiva_basis_mode={row_basis_mode!r} does not match configured "
+                            f"expected_basis_mode={self.expected_basis_mode!r}."
+                        )
+
             duration_label = int(row["duration_label"])
             if duration_label not in self.duration_classes:
                 raise ValueError(
@@ -117,7 +149,15 @@
         if suffix in {".parquet", ".pq"}:
             import pyarrow.parquet as pq
 
-            columns = list(dict.fromkeys((*self.REQUIRED_KEYS, *self.OPTIONAL_KEYS)))
+            parquet_file = pq.ParquetFile(path)
+            available = set(parquet_file.schema_arrow.names)
+            columns = [
+                key for key in dict.fromkeys((*self.REQUIRED_KEYS, *self.OPTIONAL_KEYS))
+                if key in available
+            ]
+            missing_required = [key for key in self.REQUIRED_KEYS if key not in available]
+            if missing_required:
+                raise ValueError(f"HiVA coefficient sidecar {path} is missing required columns {missing_required}.")
             return pq.read_table(path, columns=columns).to_pylist()
         if suffix in {".jsonl", ".json"}:
             with path.open("r", encoding="utf-8") as f:
@@ -188,6 +228,19 @@
             1 if str(row.get("hiva_tail_mode", "real")) == "terminal_hold" else 0,
             dtype=torch.long,
         )
+        for key in (
+            "hiva_prefix_tr_rmse",
+            "hiva_prefix_rot_rmse",
+            "hiva_prefix_grip_rmse",
+            "hiva_prefix_action_rmse",
+            "hiva_wrong_long_tr_energy",
+            "hiva_wrong_long_rot_energy",
+            "hiva_wrong_long_grip_energy",
+            "hiva_wrong_long_action_energy",
+            "hiva_tail_motion_energy",
+        ):
+            if row.get(key) not in (None, ""):
+                sample[key] = torch.tensor(float(row[key]), dtype=torch.float32)
         return sample
 
     def __getitems__(self, indices):
--- /mnt/data/hiva_hp_original/factory.py	2026-05-05 22:10:24.232565950 +0000
+++ /mnt/data/factory.py	2026-05-05 22:12:16.824597656 +0000
@@ -150,6 +150,7 @@
             n_ctrl=getattr(cfg.policy, "hiva_k"),
             duration_classes=tuple(getattr(cfg.policy, "hiva_duration_classes")),
             require_full_coverage=True,
+            expected_basis_mode=getattr(cfg.policy, "hiva_basis_mode", None),
         )
 
     return dataset
--- /mnt/data/hiva_hp_original/finetune_bigflow_ckpt_20k_hiva_coeff_cleaner_suffix.sh	2026-05-05 22:10:24.233008302 +0000
+++ /mnt/data/finetune_bigflow_ckpt_20k_hiva_coeff_cleaner_suffix.sh	2026-05-05 22:12:28.009230309 +0000
@@ -52,6 +52,7 @@
 HIVA_DURATION_NOISY_SIGMA="${HIVA_DURATION_NOISY_SIGMA:-0.25}"
 HIVA_DURATION_LOSS="${HIVA_DURATION_LOSS:-ce_mean}"
 HIVA_SUFFIX_ATTENTION="${HIVA_SUFFIX_ATTENTION:-duration_prefix}"
+HIVA_BASIS_MODE="${HIVA_BASIS_MODE:-duration_specific}"
 
 export CUDA_VISIBLE_DEVICES="${GPU_IDS}"
 export MUJOCO_GL="${MUJOCO_GL:-egl}"
@@ -125,6 +126,7 @@
 echo "HIVA_DURATION_NOISY_SIGMA=${HIVA_DURATION_NOISY_SIGMA}"
 echo "HIVA_DURATION_LOSS=${HIVA_DURATION_LOSS}"
 echo "HIVA_SUFFIX_ATTENTION=${HIVA_SUFFIX_ATTENTION}"
+echo "HIVA_BASIS_MODE=${HIVA_BASIS_MODE}"
 print_wandb_config
 
 "${ACCELERATE_BIN}" launch \
@@ -148,6 +150,7 @@
   --policy.hiva_duration_noisy_sigma="${HIVA_DURATION_NOISY_SIGMA}" \
   --policy.hiva_duration_loss="${HIVA_DURATION_LOSS}" \
   --policy.hiva_suffix_attention="${HIVA_SUFFIX_ATTENTION}" \
+  --policy.hiva_basis_mode="${HIVA_BASIS_MODE}" \
   --batch_size="${BATCH_SIZE}" \
   --steps="${STEPS}" \
   --log_freq=1 \
--- /dev/null	2026-05-05 22:08:21.787617935 +0000
+++ /mnt/data/build_hiva_coeff_sidecar_hp.py	2026-05-05 22:13:34.848006587 +0000
@@ -0,0 +1,364 @@
+#!/usr/bin/env python
+from __future__ import annotations
+
+"""Build canonical hold-pad HiVA coefficient sidecar.
+
+This builder consumes an existing frame-aligned duration sidecar whose duration labels are already in
+{6, 10, 15} (or can be mapped from legacy labels) and creates canonical-Dmax B-spline coefficient
+labels. For every sample, the GT target is hold-padded after its GT duration d*: translation and
+rotation cumulative targets hold their final value, and the absolute gripper command holds its final
+value. Coefficients are fit against one shared Dmax x K cubic B-spline basis for all durations.
+"""
+
+import argparse
+import json
+import math
+from collections import defaultdict
+from pathlib import Path
+from typing import Any
+
+import numpy as np
+import pyarrow as pa
+import pyarrow.dataset as ds
+import pyarrow.parquet as pq
+
+
+def clamped_bspline_basis(d: int, *, n_ctrl: int, degree: int) -> np.ndarray:
+    if d < 2:
+        raise ValueError(f"Duration must be >= 2, got {d}.")
+    if n_ctrl < degree + 1:
+        raise ValueError(f"n_ctrl={n_ctrl} must be at least degree+1={degree + 1}.")
+
+    x_values = [float(x) for x in range(d)]
+    internal_count = n_ctrl - degree + 1
+    if internal_count <= 1:
+        t_internal = [0.0]
+    else:
+        t_internal = [(d - 1) * i / (internal_count - 1) for i in range(internal_count)]
+    knots = [0.0] * degree + t_internal + [float(d - 1)] * degree
+
+    def basis_one(i: int, p: int, x: float) -> float:
+        if p == 0:
+            left, right = knots[i], knots[i + 1]
+            if left <= x < right:
+                return 1.0
+            if x == knots[-1] and left <= x <= right:
+                return 1.0
+            return 0.0
+        left_den = knots[i + p] - knots[i]
+        right_den = knots[i + p + 1] - knots[i + 1]
+        left_term = 0.0
+        right_term = 0.0
+        if left_den > 0:
+            left_term = (x - knots[i]) / left_den * basis_one(i, p - 1, x)
+        if right_den > 0:
+            right_term = (knots[i + p + 1] - x) / right_den * basis_one(i + 1, p - 1, x)
+        return left_term + right_term
+
+    return np.asarray([[basis_one(i, degree, x) for i in range(n_ctrl)] for x in x_values], dtype=np.float32)
+
+
+def solve_coeff(phi: np.ndarray, target: np.ndarray, ridge: float = 1e-8) -> np.ndarray:
+    # target: [Dmax, C], phi: [Dmax, K]
+    lhs = phi.T @ phi + ridge * np.eye(phi.shape[1], dtype=np.float32)
+    rhs = phi.T @ target.astype(np.float32)
+    return np.linalg.solve(lhs, rhs).astype(np.float32)
+
+
+def skew(v: np.ndarray) -> np.ndarray:
+    x, y, z = v
+    return np.array([[0.0, -z, y], [z, 0.0, -x], [-y, x, 0.0]], dtype=np.float64)
+
+
+def rotvec_to_matrix(rotvec: np.ndarray) -> np.ndarray:
+    theta = float(np.linalg.norm(rotvec))
+    k = skew(rotvec.astype(np.float64))
+    eye = np.eye(3, dtype=np.float64)
+    if theta < 1e-8:
+        return eye + k + 0.5 * (k @ k)
+    a = math.sin(theta) / theta
+    b = (1.0 - math.cos(theta)) / (theta * theta)
+    return eye + a * k + b * (k @ k)
+
+
+def matrix_to_rotvec(matrix: np.ndarray) -> np.ndarray:
+    trace = float(np.trace(matrix))
+    cos_theta = max(-1.0 + 1e-7, min(1.0 - 1e-7, 0.5 * (trace - 1.0)))
+    theta = math.acos(cos_theta)
+    omega = np.array(
+        [
+            matrix[2, 1] - matrix[1, 2],
+            matrix[0, 2] - matrix[2, 0],
+            matrix[1, 0] - matrix[0, 1],
+        ],
+        dtype=np.float64,
+    )
+    if theta < 1e-6:
+        return (0.5 * omega).astype(np.float32)
+    return ((theta / (2.0 * math.sin(theta))) * omega).astype(np.float32)
+
+
+def cumulative_command_rotation(rot_actions: np.ndarray, *, eta: float) -> np.ndarray:
+    out = []
+    cur = np.eye(3, dtype=np.float64)
+    for r in rot_actions:
+        cur = cur @ rotvec_to_matrix(float(eta) * r.astype(np.float64))
+        out.append(matrix_to_rotvec(cur))
+    return np.asarray(out, dtype=np.float32)
+
+
+def decode_actions(phi: np.ndarray, theta_tr: np.ndarray, theta_rot: np.ndarray, theta_grip: np.ndarray, *, eta: float):
+    dmax = phi.shape[0]
+    p_hat = phi @ theta_tr
+    tr_delta = np.diff(np.concatenate([np.zeros((1, 3), dtype=np.float32), p_hat], axis=0), axis=0)
+
+    rho_hat = phi @ theta_rot
+    rot_mats = np.stack([rotvec_to_matrix(r) for r in rho_hat], axis=0)
+    raw_rot = []
+    prev = np.eye(3, dtype=np.float64)
+    for mat in rot_mats:
+        delta = prev.T @ mat
+        raw_rot.append(matrix_to_rotvec(delta) / float(eta))
+        prev = mat
+    raw_rot = np.asarray(raw_rot, dtype=np.float32).reshape(dmax, 3)
+
+    grip = np.clip(phi @ theta_grip, -1.0, 1.0).astype(np.float32)
+    return np.concatenate([tr_delta.astype(np.float32), raw_rot, grip], axis=-1)
+
+
+def parse_duration_map(items: list[str] | None) -> dict[int, int]:
+    if not items:
+        return {}
+    out = {}
+    for item in items:
+        if ":" not in item:
+            raise ValueError(f"Bad duration map item {item!r}; expected old:new.")
+        old, new = item.split(":", 1)
+        out[int(old)] = int(new)
+    return out
+
+
+def load_base_actions(data_root: Path, columns: tuple[str, ...]) -> pa.Table:
+    data_dir = data_root / "data"
+    if data_dir.exists():
+        dataset = ds.dataset(str(data_dir), format="parquet", partitioning="hive")
+        return dataset.to_table(columns=list(columns))
+    # Fallback: caller may pass a parquet file or directory directly.
+    if data_root.suffix.lower() in {".parquet", ".pq"}:
+        return pq.read_table(data_root, columns=list(columns))
+    dataset = ds.dataset(str(data_root), format="parquet")
+    return dataset.to_table(columns=list(columns))
+
+
+def rmse(x: np.ndarray) -> float:
+    return float(np.sqrt(np.mean(np.square(x.astype(np.float64))))) if x.size else 0.0
+
+
+def mean_norm(x: np.ndarray) -> float:
+    return float(np.mean(np.linalg.norm(x.astype(np.float64), axis=-1))) if x.size else 0.0
+
+
+def build(args: argparse.Namespace) -> None:
+    data_root = Path(args.data_root)
+    duration_sidecar = Path(args.duration_sidecar)
+    output = Path(args.output)
+    summary_json = Path(args.summary_json) if args.summary_json else output.with_suffix(".summary.json")
+    duration_map = parse_duration_map(args.duration_map)
+    duration_classes = tuple(int(x) for x in args.duration_classes)
+    duration_to_class = {d: i for i, d in enumerate(duration_classes)}
+
+    phi_can = clamped_bspline_basis(args.dmax, n_ctrl=args.n_ctrl, degree=args.degree)
+    duration_rows = pq.read_table(duration_sidecar).to_pylist()
+    base_table = load_base_actions(data_root, columns=("index", "episode_index", "frame_index", args.action_column))
+    base_rows = base_table.to_pylist()
+
+    by_dataset_index: dict[int, dict[str, Any]] = {int(r["index"]): r for r in base_rows}
+    episode_rows: dict[int, list[dict[str, Any]]] = defaultdict(list)
+    for r in base_rows:
+        episode_rows[int(r["episode_index"])].append(r)
+    for rows in episode_rows.values():
+        rows.sort(key=lambda x: int(x["frame_index"]))
+
+    episode_actions: dict[int, np.ndarray] = {}
+    episode_frame_to_pos: dict[int, dict[int, int]] = {}
+    for ep, rows in episode_rows.items():
+        episode_actions[ep] = np.asarray([r[args.action_column] for r in rows], dtype=np.float32)
+        episode_frame_to_pos[ep] = {int(r["frame_index"]): i for i, r in enumerate(rows)}
+
+    out_rows = []
+    stats_tr = []
+    stats_rot = []
+    stats_grip = []
+    duration_hist = defaultdict(int)
+    tail_mode_hist = defaultdict(int)
+    metric_by_duration: dict[int, dict[str, list[float]]] = defaultdict(lambda: defaultdict(list))
+
+    for row in duration_rows:
+        dataset_index = int(row["dataset_index"])
+        base = by_dataset_index.get(dataset_index)
+        if base is None:
+            raise KeyError(f"dataset_index={dataset_index} from duration sidecar is missing in base dataset.")
+        ep = int(base["episode_index"])
+        frame = int(base["frame_index"])
+        pos = episode_frame_to_pos[ep][frame]
+        actions_ep = episode_actions[ep]
+        remaining = int(actions_ep.shape[0] - pos)
+        if remaining <= 0:
+            raise ValueError(f"No action remains for episode={ep}, frame={frame}.")
+
+        raw_label = int(row["duration_label"])
+        d_star = duration_map.get(raw_label, raw_label)
+        if d_star not in duration_to_class:
+            raise ValueError(f"Mapped duration {d_star} is not in {duration_classes}; raw label was {raw_label}.")
+        real_steps = min(d_star, remaining)
+        real_actions = actions_ep[pos : pos + real_steps]
+
+        # HP targets over Dmax. For unavailable or post-duration steps, translation/rotation deltas are zero,
+        # and gripper holds its final absolute command.
+        tr_delta_target = np.zeros((args.dmax, 3), dtype=np.float32)
+        tr_delta_target[:real_steps] = real_actions[:, 0:3]
+        p_hold = np.cumsum(tr_delta_target, axis=0).astype(np.float32)
+
+        rho_hold = np.zeros((args.dmax, 3), dtype=np.float32)
+        rho_real = cumulative_command_rotation(real_actions[:, 3:6], eta=args.rot_scale_eta)
+        rho_hold[:real_steps] = rho_real
+        if real_steps < args.dmax:
+            rho_hold[real_steps:] = rho_hold[real_steps - 1]
+
+        g_hold = np.zeros((args.dmax, 1), dtype=np.float32)
+        g_hold[:real_steps, 0] = real_actions[:, 6]
+        if real_steps < args.dmax:
+            g_hold[real_steps:, 0] = g_hold[real_steps - 1, 0]
+
+        theta_tr = solve_coeff(phi_can, p_hold, ridge=args.ridge)
+        theta_rot = solve_coeff(phi_can, rho_hold, ridge=args.ridge)
+        theta_grip = solve_coeff(phi_can, g_hold, ridge=args.ridge)
+
+        pred_actions = decode_actions(phi_can, theta_tr, theta_rot, theta_grip, eta=args.rot_scale_eta)
+        target_actions = np.concatenate([tr_delta_target, np.zeros((args.dmax, 3), dtype=np.float32), g_hold], axis=-1)
+        target_actions[:real_steps, 3:6] = real_actions[:, 3:6]
+
+        prefix_slice = slice(0, d_star)
+        real_slice = slice(0, real_steps)
+        prefix_err = pred_actions[prefix_slice] - target_actions[prefix_slice]
+        real_prefix_err = pred_actions[real_slice] - target_actions[real_slice]
+        if d_star < args.dmax:
+            tail = pred_actions[d_star:]
+            grip_ref = pred_actions[d_star - 1, 6]
+            wrong_long_tr = mean_norm(tail[:, 0:3])
+            wrong_long_rot = mean_norm(tail[:, 3:6])
+            wrong_long_grip = float(np.mean(np.abs(tail[:, 6] - grip_ref))) if tail.size else 0.0
+        else:
+            wrong_long_tr = wrong_long_rot = wrong_long_grip = 0.0
+        wrong_long_action = wrong_long_tr + wrong_long_rot + wrong_long_grip
+
+        out_row = {
+            "dataset_index": dataset_index,
+            "episode_index": ep,
+            "frame_index": frame,
+            "duration_label": int(d_star),
+            "duration_class": int(duration_to_class[d_star]),
+            "source_duration_label": raw_label,
+            "hiva_theta_tr_raw": theta_tr.tolist(),
+            "hiva_theta_rot_raw": theta_rot.tolist(),
+            "hiva_theta_grip_raw": theta_grip.tolist(),
+            "hiva_P_tr_hold_raw": p_hold.tolist(),
+            "hiva_rho_rot_hold_raw": rho_hold.tolist(),
+            "hiva_G_grip_hold_raw": g_hold.tolist(),
+            "hiva_basis_mode": "canonical_hp",
+            "hiva_target_mode": "hold_pad",
+            "hiva_basis_dmax": int(args.dmax),
+            "hiva_basis_k": int(args.n_ctrl),
+            "hiva_basis_degree": int(args.degree),
+            "hiva_real_steps": int(real_steps),
+            "hiva_has_synthetic_tail": bool(real_steps < d_star),
+            "hiva_tail_mode": "terminal_hold" if real_steps < d_star else "real",
+            "hiva_prefix_tr_rmse": rmse(prefix_err[:, 0:3]),
+            "hiva_prefix_rot_rmse": rmse(prefix_err[:, 3:6]),
+            "hiva_prefix_grip_rmse": rmse(prefix_err[:, 6:7]),
+            "hiva_prefix_action_rmse": rmse(prefix_err),
+            "hiva_real_prefix_action_rmse": rmse(real_prefix_err),
+            "hiva_wrong_long_tr_energy": wrong_long_tr,
+            "hiva_wrong_long_rot_energy": wrong_long_rot,
+            "hiva_wrong_long_grip_energy": wrong_long_grip,
+            "hiva_wrong_long_action_energy": wrong_long_action,
+            "hiva_tail_motion_energy": wrong_long_action,
+        }
+        out_rows.append(out_row)
+        stats_tr.append(theta_tr)
+        stats_rot.append(theta_rot)
+        stats_grip.append(theta_grip)
+        duration_hist[int(d_star)] += 1
+        tail_mode_hist[out_row["hiva_tail_mode"]] += 1
+        for k in (
+            "hiva_prefix_tr_rmse",
+            "hiva_prefix_rot_rmse",
+            "hiva_prefix_grip_rmse",
+            "hiva_prefix_action_rmse",
+            "hiva_real_prefix_action_rmse",
+            "hiva_wrong_long_action_energy",
+            "hiva_tail_motion_energy",
+        ):
+            metric_by_duration[int(d_star)][k].append(float(out_row[k]))
+
+    output.parent.mkdir(parents=True, exist_ok=True)
+    pq.write_table(pa.Table.from_pylist(out_rows), output)
+
+    tr = np.stack(stats_tr, axis=0).reshape(-1, 3)
+    rot = np.stack(stats_rot, axis=0).reshape(-1, 3)
+    grip = np.stack(stats_grip, axis=0).reshape(-1, 1)
+
+    def stat_block(arr: np.ndarray) -> dict[str, list[float]]:
+        std = arr.std(axis=0).astype(np.float64)
+        std = np.maximum(std, args.norm_eps)
+        return {"mean": arr.mean(axis=0).astype(np.float64).tolist(), "std": std.tolist()}
+
+    metrics = {}
+    for d, groups in metric_by_duration.items():
+        metrics[str(d)] = {key: float(np.mean(vals)) for key, vals in groups.items() if vals}
+
+    summary = {
+        "basis_mode": "canonical_hp",
+        "target_mode": "hold_pad",
+        "rows": len(out_rows),
+        "duration_classes": list(duration_classes),
+        "duration_hist": {str(k): int(v) for k, v in sorted(duration_hist.items())},
+        "tail_mode_hist": {str(k): int(v) for k, v in sorted(tail_mode_hist.items())},
+        "dmax": int(args.dmax),
+        "n_ctrl": int(args.n_ctrl),
+        "degree": int(args.degree),
+        "rot_scale_eta": float(args.rot_scale_eta),
+        "coef_stats": {"tr": stat_block(tr), "rot": stat_block(rot), "grip": stat_block(grip)},
+        "diagnostics_by_duration": metrics,
+        "basis_canonical": phi_can.tolist(),
+        "source_duration_sidecar": str(duration_sidecar),
+        "source_data_root": str(data_root),
+    }
+    summary_json.parent.mkdir(parents=True, exist_ok=True)
+    summary_json.write_text(json.dumps(summary, indent=2), encoding="utf-8")
+    print(f"Wrote {len(out_rows)} rows to {output}")
+    print(f"Wrote summary to {summary_json}")
+    print(json.dumps({"duration_hist": summary["duration_hist"], "tail_mode_hist": summary["tail_mode_hist"]}, indent=2))
+
+
+def main() -> None:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--data-root", required=True, help="LeRobot dataset root or parquet dataset path.")
+    parser.add_argument("--duration-sidecar", required=True, help="Existing frame-aligned duration sidecar parquet.")
+    parser.add_argument("--output", required=True, help="Output HP coefficient sidecar parquet.")
+    parser.add_argument("--summary-json", default=None, help="Output summary JSON. Defaults to output with .summary.json.")
+    parser.add_argument("--duration-classes", nargs="+", type=int, default=[6, 10, 15])
+    parser.add_argument("--duration-map", nargs="*", default=None, help="Optional old:new mappings, e.g. 1:6 3:10 8:15.")
+    parser.add_argument("--dmax", type=int, default=15)
+    parser.add_argument("--n-ctrl", type=int, default=6)
+    parser.add_argument("--degree", type=int, default=3)
+    parser.add_argument("--rot-scale-eta", type=float, default=0.5)
+    parser.add_argument("--ridge", type=float, default=1e-8)
+    parser.add_argument("--norm-eps", type=float, default=1e-6)
+    parser.add_argument("--action-column", default="action")
+    args = parser.parse_args()
+    build(args)
+
+
+if __name__ == "__main__":
+    main()
